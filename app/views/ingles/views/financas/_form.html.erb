<%= form_for(@financa) do |f| %>
  <% if @financa.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@financa.errors.count, "error") %> prohibited this financa from being saved:</h2>

      <ul>
      <% @financa.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :cabecalho %><br>
    <%= f.text_field :cabecalho %>
  </div>
  <div class="field">
    <%= f.label :texto %><br>
    <%= f.text_field :texto %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
